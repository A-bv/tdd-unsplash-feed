/// FeedKit — an image feed loader built strictly with TDD.
///
/// The composition root lives here; it is filled in once the pieces it
/// wires together have been driven out by their own tests.
public enum FeedKit {}
